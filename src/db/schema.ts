// Aucune table définie pour l'instant — le schéma (posts, collections, notes) est une
// décision produit, hors scope de cette phase d'initialisation (INIT.md §9.6).
// Rappel des 4 colonnes obligatoires sur chaque table synchronisable (INIT.md §6) :
// id (TEXT PRIMARY KEY, UUID client), created_at, updated_at, deleted_at, user_id (NULL).
export {};
